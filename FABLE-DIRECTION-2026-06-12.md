# Fable direction memo — sandkasten (2026-06-12, final-night blitz)

Sandkasten is a working open-source modern-operations sim (Next.js 16 + MapLibre + TypeScript): radar detection with track states, fog of war, missile combat with SAM defense, doctrine-scripted OPFOR AI, an LLM-driven InfoWar media feed, a scenario editor, 60k+ platform records, and a 6-test Playwright suite merged 2026-04-28. Tree is clean and the repo has been dormant since that merge. One open task: sand-007, the WeGo-niche positioning write-up (interactive_only, fully scoped in GS-private `state/sandkasten/tasks.json`). Load-bearing fact verified in code: the engine is a continuous 50ms tick loop (`simulationTick` in `src/lib/simulation/engine.ts`, driven by `useSimulation.ts`), and GDD.md §2.2 / Phase 5 already designs WeGo as a layer on top of that single-player engine, not a replacement for it.

## The calls

1. **LOCK — next move is sand-007, nothing else first.** Write `strategy/POSITIONING.md` landing the CMO-has-no-WeGo gap (CMO is real-time-with-pause; true simultaneous-resolution is unfilled in the modern operational space). The task is fully scoped and one session. The project's revival path is strategic, not technical — do not add sim features before the positioning exists.
2. **LOCK — WeGo needs no engine rebuild.** The tick engine plus waypoint orders is structurally the WeGo substrate: collect orders from both sides, run N ticks of simultaneous resolution, pause, repeat. GDD Phase 5 says exactly this ("WeGo multiplayer can be layered on top of a working single-player engine"). Treat any future "rebuild for WeGo" framing as falsified by the code that exists.
3. **LOCK — ship sequence: single-player WeGo first, multiplayer later.** An orders-phase → resolution-burst loop against the existing OPFOR AI needs zero netcode and makes the WeGo claim true on behavior. WebSocket sync, lobby, and server-side fog of war come after the turn structure is proven fun single-player.
4. **LOCK — no community surfacing until the single-player WeGo loop runs.** The pitch is "the WeGo answer CMO doesn't have"; showing up on r/wargames with WeGo still at "Planned" spends the one first impression on the exact missing piece. The rest of the sim is strong demo material — let it back the claim, not substitute for it.
5. **LOCK — the scout-flagged README fix is a non-task.** Tonight's recon brief claimed the README "Current State" section cuts off mid-list; it does not — the section is a complete Complete/Next/Planned table and the file ends cleanly. The real README gap is smaller: the hero shows a thematic sand-table photo, not a screenshot of the working sim. One capture of the play view (radar contacts, NATO symbols, weapon tracks) whenever the repo is next touched.
6. **[RAY] Audience register — my lean: hobbyist-first.** r/wargames and CMO-adjacent Discords are the people who feel the WeGo gap. The defense/OSINT crossover via Auftragstaktik import is a real second act, but a defense-flavored register invites scrutiny a pre-v1 open-source sim doesn't need.
7. **[RAY] Hero honesty — my lean: soften "WeGo multiplayer" in the README hero until call 3 ships.** The whole positioning is that CMO's turn structure under-delivers its framing; Sandkasten's own hero claiming a Planned feature is the same sin in miniature. "WeGo (in development)" costs nothing and keeps the pitch clean if anyone arrives early.

## Risks to respect

- **Dormancy is the real threat.** Zero commits since 2026-04-28; classified dormant in the GS index. The WeGo gap stays open only until someone else notices it. sand-007 is cheap insurance on a sleeper thesis — Ray's own 2026-05-05 framing.
- **The tick loop is client-side React state.** Fine for single-player WeGo; multiplayer will eventually need server-authoritative resolution for fog-of-war integrity (GDD already plans server-side fog). Do not let that future cost scare the cheap first step.
- **Known sim gaps (fuel, altitude, save/load) are real but block nothing above.** They are "Next" in the README table; keep them behind positioning and the WeGo loop in the queue.

## Fable-era note

The register is dry, military-functional, German-named — Sandkasten, Auftragstaktik — with NATO symbology and real-platform fidelity as the aesthetic spine. Keep all positioning copy specific and named (CMO, Matrix/Slitherine, "real-time-with-pause") rather than generic wargame-fan mush; the project's credibility is precision. Conventions a successor must hold: docs live at repo root as caps-named `.md` files (GDD.md, SESSION_NOTES.md, TASKS.md), not in `docs/`; the canonical task ledger is GS-private `state/sandkasten/tasks.json`, with sand-007 interactive_only because positioning is voice-bearing and Ray iterates on it. The structural reference is CMO itself — match its domain conventions (track-state realism, sensor modeling, platform data) and differentiate on turn structure alone. That is the whole thesis; do not dilute it into "another open-source wargame."
